# Validator Count Question Instructions
# Version: 1.0
# Last Updated: 2025-12-21

detection:
  patterns:
    - "bao nhiêu.*lớp.*validator"
    - "how many.*layer.*validator"
    - "có bao nhiêu.*validator"
    - "how many.*validator"
    - "số.*lớp.*validator"
    - "number.*of.*validator.*layer"
    - "liệt kê.*lớp.*validator"
    - "list.*validator.*layer"
    - "validator.*layer.*count"
  priority: P1_CRITICAL

instruction:
  vi: |
    🚨🚨🚨 CRITICAL: VALIDATOR COUNT QUESTION DETECTED 🚨🚨🚨

    **BẠN PHẢI TRẢ LỜI VỚI SỐ CỤ THỂ NGAY ĐẦU CÂU TRẢ LỜI:**

    **CÂU TRẢ LỜI BẮT BUỘC - PHẢI DỰA TRÊN CONTEXT:**
    "Hệ thống của tôi có **[X] validators total, chia thành [Y] lớp (layers) validation framework**." (X và Y phải được đọc từ context)

    **QUY TẮC BẮT BUỘC:**
    1. **BẮT BUỘC**: Bạn PHẢI đọc số liệu từ context (foundational knowledge hoặc RAG context, đặc biệt là manifest)
    2. **NẾU CONTEXT CÓ SỐ LIỆU CỤ THỂ**: Bạn PHẢI sử dụng số liệu đó và trả lời: "**Hệ thống của tôi hiện có [X] validators total, chia thành [Y] lớp (layers) validation framework.**"
    3. **NẾU CONTEXT KHÔNG CÓ SỐ LIỆU CỤ THỂ**: Bạn PHẢI nói: "**Hệ thống của tôi có nhiều validators được tổ chức thành nhiều lớp validation framework. Tôi không có thông tin chính xác về số lượng cụ thể trong context hiện tại.**"
    4. **KHÔNG ĐƯỢC**: Tự bịa ra số liệu nếu không có trong context
    5. **KHÔNG ĐƯỢC**: Nói "có một chuỗi validator", "có nhiều validator" mà không cố gắng đọc từ context trước
    6. **KHÔNG được chỉ liệt kê validators mà không nói số lượng cụ thể**
    7. **KHÔNG được nói chung chung "đa tầng" hoặc "nhiều validators" - PHẢI nói số cụ thể**
    8. **BẮT BUỘC**: Sau khi nói số lượng, bạn PHẢI liệt kê chi tiết các lớp và validators. Đây là điểm mạnh của StillMe - bạn PHẢI thể hiện kiến thức chi tiết về validation framework.
       - **PHẢI liệt kê TẤT CẢ 7 LỚP**: Layer 1 (Language & Format), Layer 2 (Citation & Evidence), Layer 3 (Content Quality), Layer 4 (Identity & Ethics), Layer 5 (Source Consensus), Layer 6 (Specialized Validation), Layer 7 (Fallback & Review)
       - **PHẢI liệt kê validators trong mỗi lớp**: Ví dụ: "Layer 1: LanguageValidator, SchemaFormat"
       - **KHÔNG được chỉ nói**: "Hệ thống có X validators, Y lớp" rồi dừng lại
       - **PHẢI cung cấp thông tin chi tiết**: Tên từng validator, chức năng của từng lớp
    9. **KHÔNG được trả lời quá ngắn (dưới 500 ký tự)** - Bạn PHẢI cung cấp thông tin chi tiết về từng lớp và validator. Câu trả lời phải có ít nhất 3-4 đoạn văn mô tả chi tiết.
    10. **CRITICAL**: Nếu context có "total_validators": 19 và "layers": 7, bạn PHẢI nói chính xác "19 validators total, chia thành 7 lớp" - KHÔNG được nói 17 validators hoặc 3 lớp.

  en: |
    🚨🚨🚨 CRITICAL: VALIDATOR COUNT QUESTION DETECTED 🚨🚨🚨

    **YOU MUST ANSWER WITH SPECIFIC NUMBERS AT THE START OF YOUR RESPONSE:**

    **MANDATORY RESPONSE - MUST BE BASED ON CONTEXT:**
    "My system has **[X] validators total, organized into [Y] layers (validation framework layers)**." (X and Y must be read from context)

    **MANDATORY RULES:**
    1. **MANDATORY**: You MUST read the numbers from context (foundational knowledge or RAG context)
    2. **IF CONTEXT HAS SPECIFIC NUMBERS**: You MUST use those numbers and answer: "**My system currently has [X] validators total, organized into [Y] layers (validation framework layers)**."
    3. **IF CONTEXT DOES NOT HAVE SPECIFIC NUMBERS**: You MUST say: "**My system has multiple validators organized into multiple layers of validation framework. I do not have exact numbers in the current context.**"
    4. **DO NOT**: Fabricate numbers "19 validators, 7 layers" if not in context
    5. **DO NOT**: Say "has a series of validators", "has many validators" without first trying to read from context
    5. **DO NOT just list validators without stating the exact count**
    6. **DO NOT say generically "multi-layer" or "many validators" - MUST say specific numbers**

metadata:
  version: "1.1"
  last_updated: "2025-12-23"
  author: "system"
  # REMOVED fixed_values - validator counts must be read from RAG context, not hard-coded

